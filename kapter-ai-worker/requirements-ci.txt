# CI runs the worker in mock mode on CPU, so it uses a lighter dependency set
# than the local CUDA-oriented requirements.txt file.
--index-url https://download.pytorch.org/whl/cpu
--extra-index-url https://pypi.org/simple

pydantic>=2.0.0
pydantic-settings>=2.0.0
numpy>=1.26.0
soundfile>=0.12.1
torch
torchaudio
torchvision
loguru>=0.7.2
fastapi>=0.115.0
python-multipart>=0.0.20
uvicorn>=0.32.0
pytest>=8.3.0
httpx>=0.27.0
