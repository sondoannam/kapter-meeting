name: CI

on:
  pull_request:
    branches:
      - development
      - main
    paths:
      - ".github/workflows/**"
      - "package.json"
      - "pnpm-lock.yaml"
      - "pnpm-workspace.yaml"
      - "libs/contracts/**"
      - "kapter-backend/**"
      - "kapter-webapp/**"
      - "kapter-extension/**"
      - "kapter-ai-worker/**"
  workflow_dispatch:

concurrency:
  group: ci-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  backend:
    uses: ./.github/workflows/backend-ci.yml
    secrets: inherit

  webapp:
    uses: ./.github/workflows/webapp-ci.yml
    secrets: inherit

  extension:
    uses: ./.github/workflows/extension-ci.yml
    secrets: inherit

  ai-worker:
    uses: ./.github/workflows/ai-worker-ci.yml
    secrets: inherit
